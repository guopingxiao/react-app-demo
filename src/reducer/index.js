
const initState = {
  inputValue: 'value',
  todolist:[1,2,3]
}

const reducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default reducer
